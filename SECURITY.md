# Security Policy

This project is pre-alpha. Security fixes target the latest main branch.

Do not publish personal IPs, tokens, credentials or private machine details in source files.

BlockForge Alpha is a local voxel sandbox first. The public project should not contain a default server address, a forced online mode, or any hidden personal connection target. If community multiplayer is explored later, it must remain optional and configurable outside the public source.
